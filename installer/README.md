# SafeSpeak installer

The release installer will package the self-contained Windows application, local moderation assets, documentation, and Elgato Stream Deck plug-in. Installation must not include secrets or user-specific configuration.

