{
  "name": "safespeak-website-deploy",
  "version": "1.0.0",
  "private": true,
  "description": "Allowlisted FTPS deployment helper for the SafeSpeak privacy and support website",
  "main": "deploy.js",
  "scripts": {
    "deploy": "node deploy.js",
    "deploy:check": "node deploy.js --check",
    "deploy:dry": "node deploy.js --dry-run",
    "deploy:verify": "node deploy.js --verify-only"
  },
  "type": "commonjs",
  "dependencies": {
    "basic-ftp": "^6.0.1"
  }
}
